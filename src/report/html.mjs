import fs from "node:fs";
import path from "node:path";
import { analyzeSuite, TARGETS } from "./analyze.mjs";

const safeJson = value => JSON.stringify(value).replaceAll("<", "\\u003c");
const escapeHtml = value => String(value).replace(/[&<>"']/g, character => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" })[character]);

export function renderHtml(data) {
  return `<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>${escapeHtml(data.title)}</title>
<style>
:root{--ink:#18262f;--muted:#63737d;--line:#dce4e8;--paper:#fff;--wash:#f4f7f8;--nav:#17333f;--aws:#ef8b22;--adb:#d33b32;--ndcs:#2775b6;--offered:#66727a}*{box-sizing:border-box}html{scroll-behavior:smooth;scroll-padding-top:62px}body{margin:0;background:var(--wash);color:var(--ink);font:14px/1.5 system-ui,sans-serif}header{background:var(--nav);color:#fff;padding:38px 5vw}header h1{margin:0 0 8px;font-size:clamp(28px,4vw,38px)}header p{margin:8px 0;color:#d7e4ea}.nav{position:sticky;top:0;z-index:10;background:rgba(255,255,255,.97);border-bottom:1px solid var(--line);overflow:auto;white-space:nowrap}.nav div{max-width:1280px;margin:auto;display:flex;gap:20px;padding:0 22px}.nav a{padding:14px 0;color:#304b5b;text-decoration:none;font-weight:650}.nav a:hover{color:#000}.wrap{max-width:1280px;margin:auto;padding:22px}.card,section{background:var(--paper);border:1px solid var(--line);border-radius:10px;padding:20px;margin-bottom:18px}section h2{margin-top:0}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:12px}.big{font-size:28px;font-weight:700}.muted,.note{color:var(--muted)}.note{font-size:13px}.callout{border-left:4px solid #4e8fae;background:#f0f7fa;padding:13px 16px;margin:14px 0}.callout h3{margin:0 0 6px}table{width:100%;border-collapse:collapse}th,td{text-align:left;padding:9px;border-bottom:1px solid var(--line);white-space:nowrap;vertical-align:top}th{background:#edf3f6;font-size:12px;text-transform:uppercase;letter-spacing:.035em}.scroll{overflow:auto;border:1px solid var(--line);border-radius:8px}.controls{display:flex;flex-wrap:wrap;gap:12px;align-items:center;margin:12px 0}.controls label{display:flex;gap:5px;align-items:center}select{padding:7px}.chart{overflow:auto;border:1px solid var(--line);border-radius:8px;background:#fff}.legend span{margin-right:16px}.dot{display:inline-block;width:10px;height:10px;border-radius:50%;margin-right:5px}details{border-top:1px solid var(--line);padding:9px 0}summary{cursor:pointer;font-weight:650}code,pre{font-size:12px}pre{overflow:auto;background:#f7f9fa;padding:12px;border-radius:7px}a{color:#126399}.evidence-group{border:1px solid var(--line);border-radius:8px;padding:12px 15px;margin:10px 0}.evidence-group h3{margin:0 0 8px}.evidence-group ul{margin:5px 0}.provider-aws{color:var(--aws);font-weight:700}.provider-adb{color:var(--adb);font-weight:700}.provider-ndcs{color:var(--ndcs);font-weight:700}@media(max-width:760px){.wrap{padding:12px}.nav div{padding:0 12px}.card,section{padding:15px}}@media print{.nav,.controls{display:none}body{background:#fff}.card,section{break-inside:avoid}}
</style>
</head>
<body>
<header><h1>${escapeHtml(data.title)}</h1><p>Generated ${data.generatedAt} · benchmark ${escapeHtml(data.benchmarkId || "not specified")}</p><p>Client-observed performance, completion, errors, elasticity and reproducibility evidence.</p></header>
<nav class="nav" aria-label="Report sections"><div>
<a href="#executive">Executive</a><a href="#methodology">Methodology</a><a href="#provisioning">Provisioning</a><a href="#results">Results</a><a href="#execution-windows">UTC windows</a><a href="#timeline">Timeline</a><a href="#errors">Errors</a><a href="#capacity">Elasticity</a><a href="#technical">Technical</a><a href="#pricing-section">Pricing</a><a href="#evidence-section">Evidence</a>
</div></nav>
<main class="wrap">
<section id="executive"><h2>Executive summary</h2><div id="conclusions" class="grid"></div><p class="muted">Successful-operation latency must be evaluated together with completion and error rates.</p></section>
<section id="methodology"><h2>Benchmark methodology</h2><div id="methodology-content"></div></section>
<section id="provisioning"><h2>Provisioned KVS capacity</h2><p>The table distinguishes customer-visible provisioned limits from provider-specific internal elasticity.</p><div id="provisioning-content"></div></section>
<section id="results"><h2>Results</h2><div id="results-content"></div></section>
<section id="execution-windows"><h2>Exact execution windows (UTC)</h2><div id="timings"></div></section>
<section id="timeline"><h2>Interactive aligned timeline</h2><p>All products use the same logical operation schedule and intended UTC timestamps. Select a session, metric, and visible series.</p><div class="controls"><label>Session <select id="session"></select></label><label>Metric <select id="metric"><option value="p99Ms">P99 latency</option><option value="p95Ms">P95 latency</option><option value="completed">Completed ops/s</option><option value="errors">Errors/s</option><option value="throttles">Throttles/s</option></select></label><label><input type="checkbox" data-series="aws" checked>AWS DynamoDB</label><label><input type="checkbox" data-series="adb" checked>ADB DynamoDB API</label><label><input type="checkbox" data-series="ndcs" checked>OCI NoSQL</label><label><input type="checkbox" data-series="offered" checked>Offered load</label></div><div class="legend"><span><i class="dot" style="background:var(--aws)"></i>AWS</span><span><i class="dot" style="background:var(--adb)"></i>ADB API</span><span><i class="dot" style="background:var(--ndcs)"></i>OCI NoSQL</span><span><i class="dot" style="background:var(--offered)"></i>Offered load</span></div><div id="chart" class="chart"></div></section>
<section id="errors"><h2>Maximums, throttling, and errors</h2><div id="windows"></div></section>
<section id="capacity"><h2>Capacity transitions</h2><div id="capacity-content"></div></section>
<section id="technical"><h2>Client concurrency and operation detail</h2><div id="technical-content"></div></section>
<section id="pricing-section"><h2>Pricing</h2><p>Database services only; the declared benchmark-window basis is shown below.</p><div id="pricing"></div></section>
<section id="evidence-section"><h2>Evidence index and reproducibility</h2><p>Links are relative so the HTML and its evidence directory can be shared together.</p><div id="evidence"></div><h3>Execution identity</h3><pre id="identity"></pre></section>
</main>
<script>
const D=${safeJson(data)};
const T=['aws','adb','ndcs'],C={aws:'#ef8b22',adb:'#d33b32',ndcs:'#2775b6',offered:'#66727a'};
const f=(v,d=3)=>v==null?'—':Number(v).toFixed(d);
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const link=(href,label)=>'<a href="'+encodeURI(href)+'">'+esc(label)+'</a>';
const provider=t=>'<span class="provider-'+t+'">'+D.labels[t]+'</span>';

function conclusions(){return D.groups.map(g=>{const ranked=T.map(t=>({t,p:g.targets[t].successfulLatency.p95})).filter(x=>x.p!=null).sort((a,b)=>a.p-b.p),best=ranked[0];return '<div class="card"><b>'+g.phase.toUpperCase()+' · '+esc(g.workload)+' · '+g.consistency+'</b><div class="big">'+(best?D.labels[best.t]:'No result')+'</div><div>Best successful P95: '+(best?f(best.p)+' ms':'—')+'</div><div class="muted">'+ranked.map(x=>D.labels[x.t]+': +'+f(best&&best.p?((x.p/best.p)-1)*100:0,1)+'%').join(' · ')+'</div></div>'}).join('')}
document.querySelector('#conclusions').innerHTML=conclusions();

function loadSteps(){const timeline=D.sessions[0]?.targets.aws.timeline||[],steps=[];let active=null;for(const point of timeline){if(point.offeredRate==null)continue;if(!active||active.rate!==point.offeredRate){if(active)active.endSecond=point.second-1;active={rate:point.offeredRate,startSecond:point.second,endSecond:point.second};steps.push(active)}else active.endSecond=point.second}return steps}
function methodology(){const first=D.sessions[0],steps=loadSteps(),phase1=D.sessions.find(s=>s.phase==='phase1'),events=phase1?.targets.aws.capacity?.events||[],strong=first?.consistency==='strong';const matrix=D.sessions.map(s=>{const x=s.targets.aws.summary,w=x.workload||{},c=x.concurrency||{};return '<tr><td>'+esc(s.phase.toUpperCase())+'</td><td>'+esc(s.workload)+'</td><td>'+esc(w.readPercent??'—')+' / '+esc(w.writePercent??'—')+'</td><td>'+esc(s.loadModel)+'</td><td>'+f(x.durationSeconds,0)+'</td><td>'+esc(c.targetConcurrency??'—')+'</td></tr>'}).join('');const defaults={phase0:'Fixed provisioned capacity; no user-requested capacity transition.',phase1:'Controlled scale-down and scale-up while the workload continues.',phase2:'Fixed-concurrency closed-loop throughput measurement.',phase3:'Capacity-covered balanced workload intended not to rely on burst.'};const phases=[...new Set(D.sessions.map(s=>s.phase))].map(p=>'<p><b>'+esc(p.toUpperCase())+':</b> '+esc(D.phaseDescriptions[p]||defaults[p]||'See suite definition.')+(p==='phase1'&&events.length?' Events: '+events.map(e=>'T+'+e.atSecond+' s ('+esc(e.name)+')').join(' and ')+'.':'')+'</p>').join('');return '<div class="grid"><div class="card"><h3>What is measured</h3><p><b>Service latency</b> wraps each database SDK call from the benchmark VM and includes same-region network plus service processing time. Queue delay is recorded separately. Percentiles use successful operations; failures retain their own latency and error evidence.</p></div><div class="card"><h3>Variable offered load</h3><p>'+steps.map(x=>x.rate+' ops/s at T+'+x.startSecond+'–'+x.endSecond+' s').join(' · ')+'</p><p class="note">Closed-loop sessions use fixed workers instead of an offered-rate schedule.</p></div><div class="card"><h3>Consistency: '+esc(first?.consistency||'not specified')+'</h3><p>'+(strong?'Strong reads explicitly request the latest committed value acknowledged before the read. They are not pooled with eventual-consistency results.':'Eventual-consistency results are kept separate from strong results.')+'</p></div></div><div class="callout"><h3>Phase definitions</h3>'+phases+'</div><h3>Session matrix</h3><div class="scroll"><table><thead><tr><th>Phase</th><th>Workload</th><th>Read/write %</th><th>Load model</th><th>Duration s</th><th>Fixed concurrency</th></tr></thead><tbody>'+matrix+'</tbody></table></div><p class="note">Acceptance requires complete raw accounting, zero client scheduler drops, accepted dataset certificates, and synchronized starts. Open-loop comparisons additionally require identical logical schedules; closed-loop comparisons require identical duration and fixed concurrency.</p>'}
document.querySelector('#methodology-content').innerHTML=methodology();

function provisioning(){const phase1=D.sessions.find(s=>s.phase==='phase1'),notes=D.capacityComparison||{},rows=T.map(t=>{const cap=phase1?.targets[t].capacity,initial=cap?.initialCapacity||{},events=cap?.events||[],custom=notes[t]||{};const baseline=custom.baseline||([initial.read,initial.write].every(Number.isFinite)?initial.read+' / '+initial.write+(initial.storageGB?' / '+initial.storageGB+' GB':''):'—');const low=custom.phase1Low||(events[0]?.requestedCapacity?events[0].requestedCapacity.read+' / '+events[0].requestedCapacity.write+(events[0].requestedCapacity.storageGB?' / '+events[0].requestedCapacity.storageGB+' GB':''):'—');const restored=custom.phase1High||(events[1]?.requestedCapacity?events[1].requestedCapacity.read+' / '+events[1].requestedCapacity.write+(events[1].requestedCapacity.storageGB?' / '+events[1].requestedCapacity.storageGB+' GB':''):baseline);return '<tr><td>'+provider(t)+'</td><td>'+esc(baseline)+'</td><td>'+esc(low)+'</td><td>'+esc(restored)+'</td><td>'+esc(custom.itemCapacity||'—')+'</td><td style="white-space:normal">'+esc(custom.behavior||'See retained capacity-event and provider evidence.')+'</td></tr>'}).join('');return '<div class="scroll"><table><thead><tr><th>Product</th><th>Phase 0 / high baseline</th><th>Phase 1 low</th><th>Restored high</th><th>Canonical item units</th><th>Service behavior</th></tr></thead><tbody>'+rows+'</tbody></table></div>'}
document.querySelector('#provisioning-content').innerHTML=provisioning();

function results(){let h='';for(const g of D.groups){h+='<h3>'+g.phase.toUpperCase()+' · '+esc(g.workload)+' · '+g.consistency+' · '+g.loadModel+'</h3><div class="scroll"><table><thead><tr><th>Product</th><th>Completed</th><th>Completion</th><th>P50 ms</th><th>P95 ms</th><th>P99 ms</th><th>P99.9 ms</th><th>Max ms</th><th>Errors</th><th>Throttle seconds</th></tr></thead><tbody>';for(const t of T){const x=g.targets[t],l=x.successfulLatency;h+='<tr><td>'+provider(t)+'</td><td>'+x.completed+' / '+x.scheduled+'</td><td>'+f(x.completionRate*100,3)+'%</td><td>'+f(l.p50)+'</td><td>'+f(l.p95)+'</td><td>'+f(l.p99)+'</td><td>'+f(l.p999)+'</td><td>'+f(l.max)+'</td><td>'+x.errors+'</td><td>'+x.throttleAffectedSeconds+'</td></tr>'}h+='</tbody></table></div>'}return h}
document.querySelector('#results-content').innerHTML=results();

function timings(){let rows='';for(const s of D.sessions)for(const t of T){const x=s.targets[t].summary,w=x.workload||{},c=x.concurrency||{};rows+='<tr><td>'+esc(s.phase.toUpperCase())+'</td><td>'+esc(s.repetition)+'</td><td>'+provider(t)+'</td><td>'+esc(x.scheduledStartAt||x.startAt)+'</td><td>'+esc(x.scheduledEndAt||'—')+'</td><td>'+esc(x.actualStartAt||'—')+'</td><td>'+esc(x.actualEndAt||'—')+'</td><td>'+f(x.actualDurationMs/1000)+'</td><td>'+esc(w.readPercent??'—')+' / '+esc(w.writePercent??'—')+'</td><td>'+esc(w.executionMode||c.executionMode||'concurrent')+'</td><td>'+f(c.observedAtOperationStart?.max,0)+'</td></tr>'}return '<div class="scroll"><table><thead><tr><th>Phase</th><th>Run</th><th>Product</th><th>Scheduled start</th><th>Scheduled end</th><th>Actual start</th><th>Actual end</th><th>Actual duration s</th><th>Read/write %</th><th>Mode</th><th>Max in-flight</th></tr></thead><tbody>'+rows+'</tbody></table></div>'}
document.querySelector('#timings').innerHTML=timings();

const session=document.querySelector('#session');D.sessions.forEach((s,i)=>session.add(new Option(s.phase.toUpperCase()+' · '+s.workload+' · '+s.consistency+' · '+s.repetition,i)));const visible={aws:true,adb:true,ndcs:true,offered:true};document.querySelectorAll('[data-series]').forEach(x=>x.onchange=()=>{visible[x.dataset.series]=x.checked;chart()});session.onchange=chart;document.querySelector('#metric').onchange=chart;
function chart(){const s=D.sessions[session.value||0],metric=document.querySelector('#metric').value,W=1120,H=340,p={l:62,r:22,t:20,b:38},series=T.filter(t=>visible[t]).map(t=>({t,v:s.targets[t].timeline.map(x=>x[metric])})),off=s.targets.aws.timeline.map(x=>x.offeredRate);let vals=series.flatMap(x=>x.v.filter(Number.isFinite));if(metric==='completed'&&visible.offered)vals.push(...off.filter(Number.isFinite));const max=Math.max(1,...vals),length=Math.max(...series.map(x=>x.v.length),off.length),x=i=>p.l+(W-p.l-p.r)*i/Math.max(1,length-1),y=v=>p.t+(H-p.t-p.b)*(1-(v||0)/max);let h='<svg viewBox="0 0 '+W+' '+H+'" role="img">';for(let i=0;i<5;i++){const yy=p.t+(H-p.t-p.b)*i/4;h+='<line x1="'+p.l+'" y1="'+yy+'" x2="'+(W-p.r)+'" y2="'+yy+'" stroke="#dce4e8"/><text x="'+(p.l-8)+'" y="'+(yy+4)+'" text-anchor="end" font-size="11">'+f(max*(1-i/4),1)+'</text>'}if(s.phase==='phase1')for(const e of s.targets.aws.capacity?.events||[]){const sec=e.atSecond;h+='<line x1="'+x(sec)+'" y1="'+p.t+'" x2="'+x(sec)+'" y2="'+(H-p.b)+'" stroke="#263b47" stroke-dasharray="6 5"/><text x="'+(x(sec)+5)+'" y="35" font-size="11">'+esc(e.name)+'</text>'}if(metric==='completed'&&visible.offered)h+='<polyline fill="none" stroke="'+C.offered+'" stroke-width="2" stroke-dasharray="5 5" points="'+off.map((v,i)=>x(i)+','+y(v)).join(' ')+'"/>';for(const z of series)h+='<polyline fill="none" stroke="'+C[z.t]+'" stroke-width="1.8" points="'+z.v.map((v,i)=>x(i)+','+y(v)).join(' ')+'"/>';const tick=Math.max(1,Math.ceil(length/50)*10);for(let sec=0;sec<length;sec+=tick)h+='<text x="'+x(sec)+'" y="'+(H-12)+'" text-anchor="middle" font-size="11">T+'+Math.floor(sec/60)+':'+String(sec%60).padStart(2,'0')+'</text>';document.querySelector('#chart').innerHTML=h+'</svg>'}
chart();

function windows(){let h='';for(const s of D.sessions)for(const t of T){const x=s.targets[t],entries=Object.entries(x.errorCounts);h+='<details><summary>'+s.phase.toUpperCase()+' · '+esc(s.repetition)+' · '+D.labels[t]+' — max '+f(x.maximum?.serviceLatencyMs)+' ms; '+x.throttling.count+' throttles over '+x.throttling.affectedSeconds+' s (longest '+x.throttling.longestSeconds+' s)</summary>'+(entries.length?'<div class="scroll"><table><thead><tr><th>Error</th><th>Count</th><th>Intervals</th></tr></thead><tbody>'+entries.map(([n,c])=>'<tr><td>'+esc(n)+'</td><td>'+c+'</td><td>'+x.errorIntervals[n].map(i=>esc(i.startAt)+' → '+esc(i.endAt)+' ('+i.durationSeconds+' s)').join('<br>')+'</td></tr>').join('')+'</tbody></table></div>':'<p>No recorded errors.</p>')+'</details>'}return h}
document.querySelector('#windows').innerHTML=windows();

function capacity(){let rows='';for(const s of D.sessions)for(const t of T)for(const e of s.targets[t].capacity?.events||[])rows+='<tr><td>'+esc(s.repetition)+'</td><td>'+provider(t)+'</td><td>'+esc(e.name)+'</td><td>'+esc(e.scheduledAt)+'</td><td>'+f(e.requestSkewMs,0)+'</td><td>'+e.requestedCapacity.read+' / '+e.requestedCapacity.write+'</td><td>'+esc(e.appliedAt||e.failedAt||'—')+'</td><td>'+f(e.applyDurationMs/1000)+'</td><td>'+esc(e.status)+'</td></tr>';return rows?'<div class="scroll"><table><thead><tr><th>Run</th><th>Product</th><th>Event</th><th>Scheduled UTC</th><th>Skew ms</th><th>Read/write</th><th>Finished UTC</th><th>Apply s</th><th>Status</th></tr></thead><tbody>'+rows+'</tbody></table></div>':'<p>No capacity transitions were supplied.</p>'}
document.querySelector('#capacity-content').innerHTML=capacity();

function technical(){let rows='';for(const s of D.sessions)for(const t of T){const x=s.targets[t],c=x.summary.concurrency||{},r=x.byOperation.read,w=x.byOperation.write;rows+='<tr><td>'+esc(s.phase.toUpperCase())+' '+esc(s.repetition)+'</td><td>'+provider(t)+'</td><td>'+f(r.p95)+'</td><td>'+f(r.p99)+'</td><td>'+f(w.p95)+'</td><td>'+f(w.p99)+'</td><td>'+f(c.observedAtOperationStart?.max,0)+'</td><td>'+f(c.configuredMaxInflight,0)+'</td><td>'+f(x.summary.queueDelayMs?.p99)+'</td><td>'+f(x.summary.client?.eventLoopDelayMs?.p99)+'</td><td>'+f(x.summary.schedulerDrops,0)+'</td></tr>'}return '<div class="scroll"><table><thead><tr><th>Run</th><th>Product</th><th>Read P95</th><th>Read P99</th><th>Write P95</th><th>Write P99</th><th>Observed max in-flight</th><th>Configured ceiling</th><th>Queue P99</th><th>Event-loop P99</th><th>Scheduler drops</th></tr></thead><tbody>'+rows+'</tbody></table></div>'}
document.querySelector('#technical-content').innerHTML=technical();

function pricing(){const p=D.pricing;if(!p)return '<p>Pricing was not supplied for this suite.</p>';const products=p.products||{};if(!Object.keys(products).length)return '<pre>'+esc(JSON.stringify(p,null,2))+'</pre>';let rows='';for(const t of T){const x=products[t]||{};rows+='<tr><td>'+provider(t)+'</td><td>'+esc(x.skuBasis||'—')+'</td><td>'+f(x.monthlyReferenceUsd,4)+'</td><td>'+f(x.benchmarkWindowCostUsd,6)+'</td><td>'+(x.sourceUrl?link(x.sourceUrl,'source'):'—')+'</td></tr>'}return '<p>'+esc(p.basis||'')+' · '+esc(p.licenseModel||'')+'</p><div class="scroll"><table><thead><tr><th>Product</th><th>SKU basis</th><th>Monthly reference '+esc(p.currency||'USD')+'</th><th>Benchmark window '+esc(p.currency||'USD')+'</th><th>Evidence</th></tr></thead><tbody>'+rows+'</tbody></table></div>'}
document.querySelector('#pricing').innerHTML=pricing();

function evidence(){let h='<div class="evidence-group"><h3>Suite and dataset identity</h3><ul><li>'+link(D.suiteEvidencePath,'Suite definition')+'</li>'+D.datasetCertificates.map((c,i)=>'<li>'+link(c.path,'Dataset certificate '+(i+1)+' — '+(c.target||'target'))+' · hash '+esc(c.observedSha256||'not recorded')+'</li>').join('')+'</ul></div>';for(const s of D.sessions){h+='<div class="evidence-group"><h3>'+esc(s.phase.toUpperCase())+' · '+esc(s.repetition)+' · '+esc(s.consistency)+'</h3><ul>';for(const t of T){const x=s.targets[t],base=x.evidencePath;h+='<li>'+provider(t)+': '+link(base+'/summary.json','summary')+' · '+link(base+'/operations.ndjson','operations NDJSON')+' · '+link(base+'/telemetry.ndjson','client telemetry')+' · '+link(base+'/run-config.json','run configuration')+(x.capacityEvidencePath?' · '+link(x.capacityEvidencePath,'capacity events'):'')+'</li>'}h+='</ul></div>'}return h}
document.querySelector('#evidence').innerHTML=evidence();
document.querySelector('#identity').textContent=JSON.stringify({benchmarkId:D.benchmarkId,scope:D.scope,capacityComparison:D.capacityComparison,references:D.references},null,2);
</script>
</body>
</html>`;
}

export function generateReport({ suite, output }) {
  const suitePath = path.resolve(suite), outputPath = path.resolve(output), outputRoot = path.dirname(outputPath); const data = analyzeSuite(suitePath);
  data.suiteEvidencePath = path.relative(outputRoot, suitePath).replaceAll("\\", "/");
  for (const certificate of data.datasetCertificates) if (certificate.path) certificate.path = path.relative(outputRoot, path.resolve(path.dirname(suitePath), certificate.path)).replaceAll("\\", "/");
  for (const session of data.sessions) for (const target of TARGETS) {
    session.targets[target].evidencePath = path.relative(outputRoot, path.resolve(path.dirname(suitePath), session.targets[target].evidencePath)).replaceAll("\\", "/");
    if (session.targets[target].capacityEvidencePath) session.targets[target].capacityEvidencePath = path.relative(outputRoot, path.resolve(path.dirname(suitePath), session.targets[target].capacityEvidencePath)).replaceAll("\\", "/");
  }
  fs.mkdirSync(outputRoot, { recursive: true }); fs.writeFileSync(outputPath, renderHtml(data)); return data;
}
