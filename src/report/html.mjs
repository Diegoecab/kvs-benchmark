import fs from "node:fs";
import path from "node:path";
import { analyzeSuite, TARGETS } from "./analyze.mjs";

const safeJson = value => JSON.stringify(value).replaceAll("<", "\\u003c");
const escapeHtml = value => String(value).replace(/[&<>"']/g, character => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" })[character]);

export function renderHtml(data) {
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${escapeHtml(data.title)}</title><style>
:root{--ink:#18262f;--muted:#63737d;--line:#dce4e8;--paper:#fff;--wash:#f4f7f8;--aws:#ef8b22;--adb:#d33b32;--ndcs:#2775b6;--offered:#66727a}*{box-sizing:border-box}body{margin:0;background:var(--wash);color:var(--ink);font:14px/1.45 system-ui,sans-serif}header{background:#17333f;color:white;padding:34px 5vw}header h1{margin:0 0 8px;font-size:30px}.wrap{max-width:1280px;margin:auto;padding:22px}.card,section{background:var(--paper);border:1px solid var(--line);border-radius:10px;padding:20px;margin-bottom:18px}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:12px}.big{font-size:28px;font-weight:700}.muted{color:var(--muted)}table{width:100%;border-collapse:collapse}th,td{text-align:left;padding:9px;border-bottom:1px solid var(--line);white-space:nowrap}.scroll{overflow:auto}.controls{display:flex;flex-wrap:wrap;gap:12px;align-items:center;margin:12px 0}.controls label{display:flex;gap:5px;align-items:center}select{padding:7px}.chart{overflow:auto;border:1px solid var(--line);border-radius:8px;background:#fff}.legend span{margin-right:16px}.dot{display:inline-block;width:10px;height:10px;border-radius:50%;margin-right:5px}details{border-top:1px solid var(--line);padding:9px 0}code{font-size:12px}a{color:#126399}@media print{.controls{display:none}body{background:#fff}.card,section{break-inside:avoid}}
</style></head><body><header><h1>${escapeHtml(data.title)}</h1><div>Generated ${data.generatedAt} · benchmark ${escapeHtml(data.benchmarkId || "not specified")}</div></header><main class="wrap"><section><h2>Executive summary</h2><div id="conclusions" class="grid"></div><p class="muted">Latency is measured around each database SDK operation from the benchmark client. Completion and errors must be evaluated with latency.</p></section><section><h2>Results</h2><div id="results"></div></section><section><h2>Interactive timeline</h2><div class="controls"><label>Session <select id="session"></select></label><label>Metric <select id="metric"><option value="p99Ms">P99 latency</option><option value="p95Ms">P95 latency</option><option value="completed">Completed ops/s</option><option value="errors">Errors/s</option><option value="throttles">Throttles/s</option></select></label><label><input type="checkbox" data-series="aws" checked>AWS DynamoDB</label><label><input type="checkbox" data-series="adb" checked>ADB DynamoDB API</label><label><input type="checkbox" data-series="ndcs" checked>OCI NoSQL</label><label><input type="checkbox" data-series="offered" checked>Offered load</label></div><div class="legend"><span><i class="dot" style="background:var(--aws)"></i>AWS</span><span><i class="dot" style="background:var(--adb)"></i>ADB API</span><span><i class="dot" style="background:var(--ndcs)"></i>OCI NoSQL</span><span><i class="dot" style="background:var(--offered)"></i>Offered load</span></div><div id="chart" class="chart"></div></section><section><h2>Maximums, throttling, and errors</h2><div id="windows"></div></section><section><h2>Capacity transitions</h2><div id="capacity"></div></section><section><h2>Client concurrency and operation detail</h2><div id="technical"></div></section><section><h2>Pricing</h2><p>Database services only; the declared benchmark-window basis is shown below.</p><pre id="pricing"></pre></section><section><h2>Evidence and reproducibility</h2><p>Evidence links are relative so this HTML can travel with the package.</p><div id="evidence"></div><h3>Execution identity</h3><pre id="identity"></pre></section></main><script>const D=${safeJson(data)};const T=['aws','adb','ndcs'],C={aws:'#ef8b22',adb:'#d33b32',ndcs:'#2775b6',offered:'#66727a'};const f=(v,d=3)=>v==null?'—':Number(v).toFixed(d);const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function conclusions(){return D.groups.map(g=>{const ranked=T.map(t=>({t,p:g.targets[t].successfulLatency.p95})).filter(x=>x.p!=null).sort((a,b)=>a.p-b.p),best=ranked[0];return '<div class="card"><b>'+g.phase.toUpperCase()+' · '+g.consistency+'</b><div class="big">'+(best?D.labels[best.t]:'No result')+'</div><div>Best P95: '+(best?f(best.p)+' ms':'—')+'</div><div class="muted">'+ranked.map(x=>D.labels[x.t]+': +'+f(best&&best.p?((x.p/best.p)-1)*100:0,1)+'%').join(' · ')+'</div></div>'}).join('')}document.querySelector('#conclusions').innerHTML=conclusions();
function results(){let h='';for(const g of D.groups){h+='<h3>'+g.phase.toUpperCase()+' · '+g.consistency+'</h3><div class="scroll"><table><thead><tr><th>Product</th><th>Completed</th><th>Completion</th><th>P50 ms</th><th>P95 ms</th><th>P99 ms</th><th>P99.9 ms</th><th>Max ms</th><th>Errors</th><th>Throttle seconds</th></tr></thead><tbody>';for(const t of T){const x=g.targets[t],l=x.successfulLatency;h+='<tr><td>'+D.labels[t]+'</td><td>'+x.completed+' / '+x.scheduled+'</td><td>'+f(x.completionRate*100,3)+'%</td><td>'+f(l.p50)+'</td><td>'+f(l.p95)+'</td><td>'+f(l.p99)+'</td><td>'+f(l.p999)+'</td><td>'+f(l.max)+'</td><td>'+x.errors+'</td><td>'+x.throttleAffectedSeconds+'</td></tr>'}h+='</tbody></table></div>'}return h}document.querySelector('#results').innerHTML=results();
const session=document.querySelector('#session');D.sessions.forEach((s,i)=>session.add(new Option(s.phase.toUpperCase()+' · '+s.consistency+' · '+s.repetition,i)));const visible={aws:true,adb:true,ndcs:true,offered:true};document.querySelectorAll('[data-series]').forEach(x=>x.onchange=()=>{visible[x.dataset.series]=x.checked;chart()});session.onchange=chart;document.querySelector('#metric').onchange=chart;
function chart(){const s=D.sessions[session.value||0],metric=document.querySelector('#metric').value,W=1120,H=340,p={l:62,r:22,t:20,b:38},series=T.filter(t=>visible[t]).map(t=>({t,v:s.targets[t].timeline.map(x=>x[metric])})),off=s.targets.aws.timeline.map(x=>x.offeredRate);let vals=series.flatMap(x=>x.v.filter(Number.isFinite));if(metric==='completed'&&visible.offered)vals.push(...off.filter(Number.isFinite));const max=Math.max(1,...vals),length=Math.max(...series.map(x=>x.v.length),off.length),x=i=>p.l+(W-p.l-p.r)*i/Math.max(1,length-1),y=v=>p.t+(H-p.t-p.b)*(1-(v||0)/max);let h='<svg viewBox="0 0 '+W+' '+H+'" role="img">';for(let i=0;i<5;i++){const yy=p.t+(H-p.t-p.b)*i/4;h+='<line x1="'+p.l+'" y1="'+yy+'" x2="'+(W-p.r)+'" y2="'+yy+'" stroke="#dce4e8"/><text x="'+(p.l-8)+'" y="'+(yy+4)+'" text-anchor="end" font-size="11">'+f(max*(1-i/4),1)+'</text>'}if(s.phase==='phase1')for(const e of s.targets.aws.capacity?.events||[]){const sec=e.atSecond;h+='<line x1="'+x(sec)+'" y1="'+p.t+'" x2="'+x(sec)+'" y2="'+(H-p.b)+'" stroke="#263b47" stroke-dasharray="6 5"/><text x="'+(x(sec)+5)+'" y="35" font-size="11">'+esc(e.name)+'</text>'}if(metric==='completed'&&visible.offered)h+='<polyline fill="none" stroke="'+C.offered+'" stroke-width="2" stroke-dasharray="5 5" points="'+off.map((v,i)=>x(i)+','+y(v)).join(' ')+'"/>';for(const z of series)h+='<polyline fill="none" stroke="'+C[z.t]+'" stroke-width="1.8" points="'+z.v.map((v,i)=>x(i)+','+y(v)).join(' ')+'"/>';const tick=Math.max(1,Math.ceil(length/50)*10);for(let sec=0;sec<length;sec+=tick)h+='<text x="'+x(sec)+'" y="'+(H-12)+'" text-anchor="middle" font-size="11">T+'+Math.floor(sec/60)+':'+String(sec%60).padStart(2,'0')+'</text>';document.querySelector('#chart').innerHTML=h+'</svg>'}chart();
function windows(){let h='';for(const s of D.sessions)for(const t of T){const x=s.targets[t];h+='<details><summary>'+s.phase.toUpperCase()+' · '+esc(s.repetition)+' · '+D.labels[t]+' — max '+f(x.maximum?.serviceLatencyMs)+' ms; '+x.throttling.count+' throttles over '+x.throttling.affectedSeconds+' s (longest '+x.throttling.longestSeconds+' s)</summary><div class="scroll"><table><thead><tr><th>Error</th><th>Count</th><th>Intervals</th></tr></thead><tbody>'+Object.entries(x.errorCounts).map(([n,c])=>'<tr><td>'+esc(n)+'</td><td>'+c+'</td><td>'+x.errorIntervals[n].map(i=>esc(i.startAt)+' → '+esc(i.endAt)+' ('+i.durationSeconds+' s)').join('<br>')+'</td></tr>').join('')+'</tbody></table></div></details>'}return h||'<p>No errors.</p>'}document.querySelector('#windows').innerHTML=windows();
function capacity(){let rows='';for(const s of D.sessions)for(const t of T)for(const e of s.targets[t].capacity?.events||[])rows+='<tr><td>'+esc(s.repetition)+'</td><td>'+D.labels[t]+'</td><td>'+esc(e.name)+'</td><td>'+esc(e.scheduledAt)+'</td><td>'+f(e.requestSkewMs,0)+'</td><td>'+e.requestedCapacity.read+' / '+e.requestedCapacity.write+'</td><td>'+esc(e.appliedAt||e.failedAt||'—')+'</td><td>'+f(e.applyDurationMs/1000)+'</td><td>'+esc(e.status)+'</td></tr>';return rows?'<div class="scroll"><table><thead><tr><th>Run</th><th>Product</th><th>Event</th><th>Scheduled UTC</th><th>Skew ms</th><th>Read/write</th><th>Finished UTC</th><th>Apply s</th><th>Status</th></tr></thead><tbody>'+rows+'</tbody></table></div>':'<p>No capacity transitions were supplied.</p>'}document.querySelector('#capacity').innerHTML=capacity();
function technical(){let rows='';for(const s of D.sessions)for(const t of T){const x=s.targets[t],c=x.summary.concurrency||{},r=x.byOperation.read,w=x.byOperation.write;rows+='<tr><td>'+esc(s.repetition)+'</td><td>'+D.labels[t]+'</td><td>'+f(r.p95)+'</td><td>'+f(r.p99)+'</td><td>'+f(w.p95)+'</td><td>'+f(w.p99)+'</td><td>'+f(c.observedAtOperationStart?.max,0)+'</td><td>'+f(c.configuredMaxInflight,0)+'</td><td>'+f(x.summary.queueDelayMs?.p99)+'</td><td>'+f(x.summary.client?.eventLoopDelayMs?.p99)+'</td><td>'+f(x.summary.schedulerDrops,0)+'</td></tr>'}return '<div class="scroll"><table><thead><tr><th>Run</th><th>Product</th><th>Read P95</th><th>Read P99</th><th>Write P95</th><th>Write P99</th><th>Observed max in-flight</th><th>Configured ceiling</th><th>Queue P99</th><th>Event-loop P99</th><th>Scheduler drops</th></tr></thead><tbody>'+rows+'</tbody></table></div>'}document.querySelector('#technical').innerHTML=technical();
function pricing(){const p=D.pricing;if(!p)return '<p>Pricing was not supplied.</p>';const products=p.products||{};if(!Object.keys(products).length)return '<pre>'+esc(JSON.stringify(p,null,2))+'</pre>';let rows='';for(const t of T){const x=products[t]||{};rows+='<tr><td>'+D.labels[t]+'</td><td>'+esc(x.skuBasis||'—')+'</td><td>'+f(x.monthlyReferenceUsd,4)+'</td><td>'+f(x.benchmarkWindowCostUsd,6)+'</td><td>'+(x.sourceUrl?'<a href="'+esc(x.sourceUrl)+'">source</a>':'—')+'</td></tr>'}return '<p>'+esc(p.basis||'')+' · '+esc(p.licenseModel||'')+'</p><div class="scroll"><table><thead><tr><th>Product</th><th>SKU basis</th><th>Monthly reference '+esc(p.currency||'USD')+'</th><th>Benchmark window '+esc(p.currency||'USD')+'</th><th>Evidence</th></tr></thead><tbody>'+rows+'</tbody></table></div>'}document.querySelector('#pricing').outerHTML='<div id="pricing">'+pricing()+'</div>';document.querySelector('#evidence').innerHTML='<ul>'+D.sessions.flatMap(s=>T.map(t=>'<li>'+esc(s.phase)+' / '+esc(s.consistency)+' / '+esc(s.repetition)+' / '+D.labels[t]+': <a href="'+encodeURI(s.targets[t].evidencePath)+'/summary.json">summary</a> · <a href="'+encodeURI(s.targets[t].evidencePath)+'/operations.ndjson">operations</a></li>')).join('')+'</ul>';document.querySelector('#identity').textContent=JSON.stringify({benchmarkId:D.benchmarkId,scope:D.scope,datasetCertificates:D.datasetCertificates,references:D.references},null,2);
const timingSection=document.createElement('section');timingSection.innerHTML='<h2>Exact execution windows (UTC)</h2><div id="timings"></div>';document.querySelector('#results').closest('section').after(timingSection);
function timings(){let rows='';for(const s of D.sessions)for(const t of T){const x=s.targets[t].summary,w=x.workload||{},c=x.concurrency||{};rows+='<tr><td>'+esc(s.phase.toUpperCase())+'</td><td>'+esc(s.repetition)+'</td><td>'+D.labels[t]+'</td><td>'+esc(x.scheduledStartAt||x.startAt)+'</td><td>'+esc(x.scheduledEndAt||'â€”')+'</td><td>'+esc(x.actualStartAt||'â€”')+'</td><td>'+esc(x.actualEndAt||'â€”')+'</td><td>'+f(x.actualDurationMs/1000)+'</td><td>'+esc(w.readPercent??'â€”')+' / '+esc(w.writePercent??'â€”')+'</td><td>'+esc(w.executionMode||c.executionMode||'concurrent')+'</td><td>'+f(c.observedAtOperationStart?.max,0)+'</td></tr>'}return '<div class="scroll"><table><thead><tr><th>Phase</th><th>Run</th><th>Product</th><th>Scheduled start</th><th>Scheduled end</th><th>Actual start</th><th>Actual end</th><th>Actual duration s</th><th>Read/write %</th><th>Mode</th><th>Max in-flight</th></tr></thead><tbody>'+rows+'</tbody></table></div>'}document.querySelector('#timings').innerHTML=timings();
</script></body></html>`;
}

export function generateReport({ suite, output }) {
  const suitePath = path.resolve(suite), outputPath = path.resolve(output); const data = analyzeSuite(suitePath);
  for (const session of data.sessions) for (const target of TARGETS) session.targets[target].evidencePath = path.relative(path.dirname(outputPath), path.resolve(path.dirname(suitePath), session.targets[target].evidencePath)).replaceAll("\\", "/");
  fs.mkdirSync(path.dirname(outputPath), { recursive: true }); fs.writeFileSync(outputPath, renderHtml(data)); return data;
}
